# JXA Builder

### Requirements

The build process requires a number of global npm packages:

```
npm run reqs
```

### Creating a new app

```
npm run app:create
```

### Making code changes and recompiling

```
npm run app
```
